---
import GlowText from '../ui/GlowText.astro';
---

<section id="about" class="about">
  <div class="container">
    <h2 class="section-title">
      <GlowText as="span" color="green">&gt;_ About Me</GlowText>
    </h2>

    <div class="about__content">
      <div class="about__image">
        <div class="about__image-wrapper">
          <div class="about__avatar">
            <div class="about__avatar-ring"></div>
            <div class="about__avatar-ring about__avatar-ring--2"></div>
            <div class="about__avatar-core">
              <span class="about__avatar-text">MD</span>
              <div class="about__avatar-pulse"></div>
            </div>
            <div class="about__avatar-particles">
              <span></span><span></span><span></span><span></span>
              <span></span><span></span><span></span><span></span>
            </div>
          </div>
          <div class="about__image-frame"></div>
          <div class="about__image-glow"></div>
        </div>
        <div class="about__status">
          <span class="about__status-dot"></span>
          <span class="about__status-text">NEURAL_LINK_ACTIVE</span>
        </div>
      </div>

      <div class="about__text">
        <div class="about__terminal">
          <div class="about__terminal-header">
            <span class="about__terminal-dot"></span>
            <span class="about__terminal-dot"></span>
            <span class="about__terminal-dot"></span>
            <span class="about__terminal-title">meelad@neural:~$</span>
          </div>
          <div class="about__terminal-body">
            <p class="about__intro">
              <span class="terminal-prompt">&gt;</span> AI/ML Engineer specializing in production-grade intelligent systems that solve real-world problems.
            </p>

            <p>
              <span class="terminal-prompt">&gt;</span> Currently at <strong>Zirak srl</strong> building real-time AI solutions: meeting assistants with sub-2s latency, RAG pipelines with LangGraph, and agentic workflows with multi-provider LLMs.
            </p>

            <p>
              <span class="terminal-prompt">&gt;</span> Pursuing M.Sc. at <strong>Politecnico di Torino</strong> with thesis research on hyperbolic compositionality in vision-language models at <strong>University of Twente</strong>.
            </p>

            <p>
              <span class="terminal-prompt">&gt;</span> Published at <strong>PROFES 2025</strong> on LLM-assisted code refactoring. Open to industry opportunities, research collaborations, and PhD positions.
            </p>
          </div>
        </div>

        <div class="about__stats">
          <div class="about__stat">
            <span class="about__stat-value">3+</span>
            <span class="about__stat-label">Years Experience</span>
          </div>
          <div class="about__stat">
            <span class="about__stat-value">1</span>
            <span class="about__stat-label">Publication</span>
          </div>
          <div class="about__stat">
            <span class="about__stat-value">8+</span>
            <span class="about__stat-label">AI Projects</span>
          </div>
          <div class="about__stat">
            <span class="about__stat-value">5+</span>
            <span class="about__stat-label">RAG Systems</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about {
    padding: var(--section-padding) 0;
    background: var(--bg-secondary);
    position: relative;
  }

  .section-title {
    text-align: center;
    margin-bottom: 4rem;
    font-family: var(--font-mono);
  }

  .about__content {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: 4rem;
    align-items: start;
  }

  .about__image {
    position: relative;
  }

  .about__image-wrapper {
    position: relative;
    aspect-ratio: 1;
    max-width: 280px;
    margin: 0 auto;
  }

  .about__avatar {
    width: 100%;
    height: 100%;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1;
  }

  .about__avatar-ring {
    position: absolute;
    inset: 0;
    border: 2px solid var(--neon-green);
    border-radius: 50%;
    animation: ring-rotate 8s linear infinite;
    box-shadow: 0 0 20px rgba(57, 255, 20, 0.3);
  }

  .about__avatar-ring::before {
    content: '';
    position: absolute;
    top: -4px;
    left: 50%;
    width: 8px;
    height: 8px;
    background: var(--neon-green);
    border-radius: 50%;
    box-shadow: 0 0 15px var(--neon-green);
  }

  .about__avatar-ring--2 {
    inset: 15px;
    border-color: var(--neon-pink);
    animation: ring-rotate 6s linear infinite reverse;
    box-shadow: 0 0 20px rgba(255, 0, 255, 0.3);
  }

  .about__avatar-ring--2::before {
    background: var(--neon-pink);
    box-shadow: 0 0 15px var(--neon-pink);
  }

  @keyframes ring-rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }

  .about__avatar-core {
    width: 60%;
    height: 60%;
    background: linear-gradient(135deg, var(--bg-tertiary), var(--bg-secondary));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    border: 2px solid var(--neon-green);
    box-shadow:
      inset 0 0 30px rgba(57, 255, 20, 0.2),
      0 0 40px rgba(57, 255, 20, 0.3);
  }

  .about__avatar-text {
    font-family: var(--font-mono);
    font-size: 3rem;
    font-weight: 700;
    color: var(--neon-green);
    text-shadow: var(--glow-green);
    z-index: 2;
  }

  .about__avatar-pulse {
    position: absolute;
    inset: -10px;
    border: 2px solid var(--neon-green);
    border-radius: 50%;
    animation: pulse-ring 2s ease-out infinite;
    opacity: 0;
  }

  @keyframes pulse-ring {
    0% {
      transform: scale(0.8);
      opacity: 1;
    }
    100% {
      transform: scale(1.3);
      opacity: 0;
    }
  }

  .about__avatar-particles span {
    position: absolute;
    width: 4px;
    height: 4px;
    background: var(--neon-green);
    border-radius: 50%;
    box-shadow: 0 0 10px var(--neon-green);
    animation: particle-float 3s ease-in-out infinite;
  }

  .about__avatar-particles span:nth-child(1) { top: 10%; left: 20%; animation-delay: 0s; }
  .about__avatar-particles span:nth-child(2) { top: 20%; right: 15%; animation-delay: 0.4s; }
  .about__avatar-particles span:nth-child(3) { bottom: 25%; left: 10%; animation-delay: 0.8s; }
  .about__avatar-particles span:nth-child(4) { bottom: 15%; right: 20%; animation-delay: 1.2s; }
  .about__avatar-particles span:nth-child(5) { top: 50%; left: 5%; animation-delay: 1.6s; background: var(--neon-pink); box-shadow: 0 0 10px var(--neon-pink); }
  .about__avatar-particles span:nth-child(6) { top: 40%; right: 5%; animation-delay: 2s; background: var(--neon-pink); box-shadow: 0 0 10px var(--neon-pink); }
  .about__avatar-particles span:nth-child(7) { bottom: 40%; left: 15%; animation-delay: 2.4s; }
  .about__avatar-particles span:nth-child(8) { bottom: 50%; right: 10%; animation-delay: 2.8s; background: var(--neon-cyan); box-shadow: 0 0 10px var(--neon-cyan); }

  @keyframes particle-float {
    0%, 100% { transform: translateY(0) scale(1); opacity: 0.8; }
    50% { transform: translateY(-10px) scale(1.2); opacity: 1; }
  }

  .about__status {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    margin-top: 1rem;
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--neon-green);
  }

  .about__status-dot {
    width: 8px;
    height: 8px;
    background: var(--neon-green);
    border-radius: 50%;
    animation: status-blink 1.5s ease-in-out infinite;
    box-shadow: 0 0 10px var(--neon-green);
  }

  @keyframes status-blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.3; }
  }

  .about__status-text {
    text-transform: uppercase;
    letter-spacing: 0.1em;
  }

  .about__image-frame {
    position: absolute;
    inset: -4px;
    border: 2px solid var(--neon-green);
    border-radius: var(--radius-md);
    z-index: 2;
    pointer-events: none;
    box-shadow: 0 0 20px rgba(57, 255, 20, 0.3);
  }

  .about__image-frame::before,
  .about__image-frame::after {
    content: '';
    position: absolute;
    width: 20px;
    height: 20px;
    border: 2px solid var(--neon-green);
  }

  .about__image-frame::before {
    top: -6px;
    left: -6px;
    border-right: none;
    border-bottom: none;
  }

  .about__image-frame::after {
    bottom: -6px;
    right: -6px;
    border-left: none;
    border-top: none;
  }

  .about__image-glow {
    position: absolute;
    inset: -20px;
    background: var(--neon-green);
    border-radius: var(--radius-md);
    filter: blur(40px);
    opacity: 0.3;
    z-index: 0;
  }

  .about__image-scanlines {
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(57, 255, 20, 0.05) 2px,
      rgba(57, 255, 20, 0.05) 4px
    );
    pointer-events: none;
    z-index: 3;
    border-radius: var(--radius-md);
  }

  .about__terminal {
    background: var(--bg-card);
    border: 1px solid var(--neon-green);
    border-radius: var(--radius-md);
    overflow: hidden;
    box-shadow: 0 0 30px rgba(57, 255, 20, 0.2);
  }

  .about__terminal-header {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1rem;
    background: var(--bg-tertiary);
    border-bottom: 1px solid var(--neon-green);
  }

  .about__terminal-dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--neon-green);
    opacity: 0.5;
  }

  .about__terminal-dot:first-child {
    background: var(--neon-red);
    opacity: 1;
    box-shadow: 0 0 8px var(--neon-red);
  }

  .about__terminal-dot:nth-child(2) {
    background: var(--neon-yellow);
    opacity: 1;
    box-shadow: 0 0 8px var(--neon-yellow);
  }

  .about__terminal-dot:nth-child(3) {
    background: var(--neon-green);
    opacity: 1;
    box-shadow: 0 0 8px var(--neon-green);
  }

  .about__terminal-title {
    margin-left: auto;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-muted);
  }

  .about__terminal-body {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .about__intro {
    font-size: 1.1rem;
    color: var(--text-primary);
  }

  .about__terminal-body p {
    color: var(--text-secondary);
    line-height: 1.8;
    font-family: var(--font-mono);
    font-size: 0.9rem;
  }

  .terminal-prompt {
    color: var(--neon-green);
    margin-right: 0.5rem;
    text-shadow: var(--glow-subtle);
  }

  .about__terminal-body strong {
    color: var(--neon-green);
    text-shadow: var(--glow-subtle);
  }

  .about__stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-top: 1.5rem;
  }

  .about__stat {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
    padding: 1rem;
    background: var(--bg-card);
    border: 1px solid var(--bg-tertiary);
    border-radius: var(--radius-md);
    text-align: center;
    transition: all var(--transition-fast);
  }

  .about__stat:hover {
    border-color: var(--neon-green);
    box-shadow: 0 0 25px rgba(57, 255, 20, 0.3);
  }

  .about__stat-value {
    font-family: var(--font-mono);
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--neon-green);
    text-shadow: var(--glow-green);
  }

  .about__stat-label {
    font-size: 0.75rem;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  @media (max-width: 768px) {
    .about__content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .about__image-wrapper {
      max-width: 200px;
    }

    .about__stats {
      grid-template-columns: repeat(2, 1fr);
    }
  }
</style>
